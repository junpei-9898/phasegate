# v0.341.0 公開記録

<!-- @work-item-id WI-220 -->

2026-09-20、ユーザーから「ローカルにある変更をルートのブランチにpushしてからnpmもpublishして」と明示依頼を受け、現在の変更を main / v0.341.0 として公開する作業を開始した。過去の監査にある公開未承認の状態はこの依頼で更新するが、WI の受け入れ基準や未達判定は変更しない。

絶対500ms/format-only fast-path、意味的影響の全分類、実agent読取比較の3点は未達のまま。CHANGELOG にも公開時点の制限として記載する。既存利用者設定の移行やガード解除はこの公開作業に含めない。

公開対象は `npm run pack:runtime` で作った compiled runtime を含む tarball。新規インストールの release-smoke 16件は、この公開対象の実バイト列で成功した。型検査、readiness、integrity 照合も成功。

L4 は L4-004（既存設計文書の経過日数）で FAIL。閾値変更や日付だけの更新は行わない。L4-006 skill catalog は PASS、文書乖離・参照切れの警告も残る。

全体回帰は5,594件成功、1件失敗、27件skip。失敗は公開作業開始後に別作業で作成されたWI-221の未反映を実コーパス検査が検出したもの。WI-221を含めない公開用worktreeで該当テストを再実行し成功。forksは別実行20件成功。WI-221は元の作業場所に保持し、このリリースには含めない。

通常pre-commitでL2-002（@unit不足）、テストの@story不足、L2-014（WI-220のstatus鮮度）を検出。対象3ファイルのUnit注釈と32テストの既存product story注釈を補い、WI-220追跡も保持した。テスト本文と実装の動作は変更していない。`work-items:status --apply --id WI-220` がfrontmatterをtestedへ更新したが、これは成果物・テスト存在の機械判定であり、上記3点の受け入れ条件達成やWI全体の完了宣言ではない。
